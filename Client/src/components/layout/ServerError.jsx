import React from "react";

export default function ServerError() {
  return (
    <div
      style={{
        display: "flex",
        width: "100vw",
        height: "100vh",
        justifyContent: "center",
        alignItem: "center",
      }}
    >
        Unable to load data 
    </div>
  );
}
